# Example 3.7 - ContextManagers
Methods that act as Python "context managers", opening a file reader/writer,
passing the reader/writer to a callback, and closing the file after.

```bash
./mill -i TestContextManagers.scala
```
