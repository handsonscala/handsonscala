@ upack.read(blob2)
res59: upack.Msg = Arr(
  ArrayBuffer(
    Arr(ArrayBuffer(Int32(1), Arr(ArrayBuffer()))),
    Arr(
      ArrayBuffer(
        Int32(2),
        Arr(
          ArrayBuffer(
            Obj(
              LinkedHashMap(
                Str("login") -> Str("haoyi"),
                Str("id") -> Int32(1337),
                Str("site_admin") -> True
              )
            ),
...
